<?php

namespace App\Utils;

class AppConstants
{
    public const MAX_UPLOAD_SIZE = 5;
    public const XML_EXTENSION = 'xml';
    public const XML_MIME_TYPE = ['text/xml', 'application/xml', 'application/x-xml'];
    public const XML_FIRST_NODE = 'catalog';
}